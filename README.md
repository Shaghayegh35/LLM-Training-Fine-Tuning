# LLM-data-tokenization-pipeline
Jupyter notebook for preparing and tokenizing instruction–response datasets using Hugging Face datasets and transformers. Includes prompt templating, JSONL parsing, data structuring, and train/test dataset creation for LLM fine-tuning.
